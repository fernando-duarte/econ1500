# Authentication - Server-side only variables (not exposed to browser)
AUTH_LOGIN_URL=/login
AUTH_DEFAULT_REDIRECT=/

# Auth redirects - Public variables (exposed to browser)
# These must be prefixed with NEXT_PUBLIC_ to be accessible in client components
NEXT_PUBLIC_LOGIN_REDIRECT="/"
NEXT_PUBLIC_LOGOUT_REDIRECT="/login"

# API URLs - Public variables
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Add other environment variables your app needs here
# 
# IMPORTANT:
# - Variables WITHOUT NEXT_PUBLIC_ prefix are only accessible in:
#   - Server Components
#   - Route Handlers
#   - Server Actions
#   - Middleware
#   - next.config.js
#
# - Variables WITH NEXT_PUBLIC_ prefix are:
#   - Embedded at build time
#   - Accessible anywhere (client & server)
#   - Visible in browser code (not secure for secrets)
#
# ENVIRONMENT FILES:
# - .env.local: Local overrides (not committed to Git)
# - .env.development: Development environment (npm run dev)
# - .env.production: Production environment (npm run build/start)
# - .env.test: Test environment (npm run test)
#
# PRIORITY ORDER (highest to lowest):
# - .env.local
# - .env.development or .env.production or .env.test
# - .env
